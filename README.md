# objectToCss
Collects an object into a string

## Sample:

```javascript
const miniJSS = {
  [`@media screen and (min-width: ${min}px)`]: {
    body: {
      'background-color': 'red'
    }
  }
}
```
